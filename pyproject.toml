[project]
name = "mm-radar"
version = "28.0.0"
description = "Monitor de licitaciones de Neuquén (CODINEU) con foco en arbitraje Paraguay para M&M Insumos."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
